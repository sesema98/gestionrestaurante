<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:sec="http://www.thymeleaf.org/extras/spring-security">
<body>
<th:block th:fragment="mainNav(active)">
    <nav class="navbar navbar-expand-lg navbar-modern fixed-top py-3">
        <div class="container-fluid px-4">
            <a class="navbar-brand" th:href="@{/}">Gestion Restaurante</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="mainNavbar">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" th:classappend="${active}=='dashboard' ? ' active' : ''" th:href="@{/}">
                            🏠 Inicio
                        </a>
                    </li>
                    <li class="nav-item" sec:authorize="hasRole('ADMIN')">
                        <a class="nav-link" th:classappend="${active}=='clientes' ? ' active' : ''" th:href="@{/clientes}">
                            👥 Clientes
                        </a>
                    </li>
                    <li class="nav-item" sec:authorize="hasRole('ADMIN')">
                        <a class="nav-link" th:classappend="${active}=='mesas' ? ' active' : ''" th:href="@{/mesas}">
                            🍽️ Mesas
                        </a>
                    </li>
                </ul>
                <div class="d-flex align-items-center gap-3">
                    <div class="text-end" sec:authorize="isAuthenticated()">
                        <small class="text-muted d-block">Usuario</small>
                        <span class="fw-semibold" sec:authentication="name"></span>
                    </div>
                    <form th:action="@{/logout}" method="post" class="ms-2">
                        <button class="btn btn-outline-dark rounded-pill px-3" type="submit">Cerrar sesión</button>
                    </form>
                </div>
            </div>
        </div>
    </nav>
</th:block>
</body>
</html>
